# 🚀 快速启动指南

## 方式一：使用启动脚本（推荐）

```bash
./start.sh
```

这会自动：
- ✅ 检查并安装依赖
- ✅ 启动后端 API (端口 5000)
- ✅ 启动前端开发服务器 (端口 3000)

## 方式二：手动启动

### 1. 安装依赖

```bash
# 前端依赖
npm install

# 后端依赖
pip3 install -r requirements.txt
```

### 2. 启动服务

**终端 1 - 后端：**
```bash
python3 api.py
```

**终端 2 - 前端：**
```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问：http://localhost:3000

## ⚠️ 注意事项

1. 确保 `api_key.txt` 文件存在且包含有效的 Tripo API Key
2. Python 版本需要 3.7+
3. Node.js 版本需要 16+

## 🎯 测试流程

1. 点击或拖拽上传一张图片
2. 选择生成配置（模型版本、纹理质量等）
3. 点击"开始生成 3D 模型"
4. 等待 1-3 分钟
5. 查看生成的 3D 模型
6. 点击"下载 3D 模型"保存文件

## 🐛 常见问题

### 前端无法连接后端

- 检查后端是否在 5000 端口运行
- 查看浏览器控制台的网络请求

### API Key 错误

- 确认 api_key.txt 文件存在
- 检查 API Key 是否正确且有效

### 生成失败

- 查看后端终端的日志输出
- 检查图片格式和大小是否符合要求
